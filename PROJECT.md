# Art Museum · 项目档案

> 一份把项目所有基本信息集中起来的文档。README 是给开发者看的用法说明，
> 这份是「现在到底有什么、有多大、怎么跑起来」的快照。
>
> 最后更新：2026-09-26

---

## 一、这是什么

一个**沉浸式 3D 虚拟艺术馆**，用 Three.js 做的第一人称漫游。
没有后端，纯静态，构建产物可以直接扔到任何静态托管上。

馆里挂着 **72 幅真实的公版画作**（克利夫兰艺术博物馆 Open Access），
分 **9 个展厅**陈列；走廊尽头立着一件 **Canova《珀尔修斯与美杜莎之首》**
的真 3D 扫描件（大都会博物馆 Open Access）。

平面是一条连续的动线：**门厅 → 41.5m 主廊 → 九个展厅**。没有传送、没有加载画面，
进门就是馆内，一路走过去。

- **线上地址**：https://mumuxi-fish.github.io/art-museum/
- **仓库**：https://github.com/mumuxi-fish/art-museum

---

## 二、技术栈

| | |
|---|---|
| 渲染 | Three.js（WebGL） |
| 构建 | Vite 5 |
| 语言 | 原生 ES 模块 + CSS，无框架 |
| 音频 | Web Audio API（纯合成，无音频文件：底噪 / 脚步 / 混响 / 生成式背景音乐） |
| 3D 模型 | glTF + Draco 压缩 |
| 部署 | GitHub Pages（GitHub Actions 自动构建） |
| 图片 | WebP（画作）、JPEG（分享预览图） |

**没有后端，没有数据库，没有运行时外部依赖**：所有资源都是本地相对路径，构建产物自己就是一个完整的站点。

---

## 三、规模

| 项目 | 数值 |
|---|---|
| 空间 | 11 个（1 门厅 + 1 主廊 + 9 展厅） |
| 画作 | 72 幅（9 厅 × 8 幅） |
| 长凳 | 12 条 |
| 门洞 | 10 个 |
| 灯光 | 全馆 117 盏（当前厅全开 + 按距离补 4 盏，实测同时点亮 ≤ 16 盏） |
| 源码 | 约 5,070 行（JS + CSS + HTML） |
| 工具脚本 | 约 1,700 行（Python + Node） |
| `dist` 体积 | 约 13 MB |
| 其中画作 1200px | 8.1 MB（只在详情浮层按需取） |
| 其中画作 640px | 2.2 MB（挂墙用，进馆即加载） |
| 其中雕塑 GLB | 1.0 MB |
| 其中 Draco 解码器 | 752 KB |
| 其中 JS + CSS | 656 KB |

---

## 四、九个展厅

前五厅**按题材**分，后四厅**按画派**分。时代从 16 世纪一路排到 20 世纪。

| # | id | 名称 | 位置 | 尺寸 (m) | 层高 | 年代 | 代表画家 |
|---|---|---|---|---|---|---|---|
| 一 | `dawn` | 光与河岸 | 主廊北 | 12.5×15.0 | 7.4 | 1855–1915 | 莫奈、毕沙罗、西斯莱 |
| 二 | `sun` | 日常与肖像 | 主廊北 | 14.0×16.5 | 7.0 | 1864–1905 | 雷诺阿、德加、马奈 |
| 三 | `minimal` | 浮世绘 | 主廊南 | 14.0×13.5 | 7.8 | 1788–1857 | 葛饰北斋、歌川广重 |
| 四 | `night` | 夜色与海 | 主廊南 | 14.0×14.0 | 7.6 | 1640–1900 | 惠斯勒、丘奇 |
| 五 | `flora` | 花与静物 | 主廊东端 | 9.5×16.5 | 7.0 | 1550–1927 | 雷东、蒙德里安 |
| 六 | `dutch` | 荷兰黄金时代 | 门厅北 | 12.0×14.5 | 7.2 | 1625–1660 | 伦勃朗、哈尔斯、鲁伊斯达尔 |
| 七 | `barbizon` | 巴比松与写实 | 门厅南 | 12.0×11.5 | 7.0 | 1800–1873 | 柯罗、库尔贝 |
| 八 | `postimp` | 后印象与纳比 | 主廊北 | 8.0×16.5 | 7.4 | 1889–1908 | 塞尚、高更、维亚尔 |
| 九 | `american` | 美国绘画 | 主廊南 | 6.5×14.0 | 7.0 | 1849–1890 | 英尼斯、荷马、蔡斯 |

每个厅的配色、层高、地面材质各不相同（石地/木地），是按各画派的观展习惯定的——
比如荷兰厅用深墙配金框，因为十七世纪绘画靠一束侧光造型，浅色墙会把那束光吃掉。

### 平面

```
z=0    ┌──────────┬────────────┬──────┐
       │ 荷兰黄金  │  光与河岸   │日常与 │ 后印象 │
       │   时代    │            │ 肖像  │  纳比  │
z=14.5 ├──────────┴────────────┤      │       │
       │  门厅  │    主廊       ├──────┤───────┤
z=21   ├───────┴──────┬────────┤ 花与 │ 美国  │
       │ 巴比松与写实  │ 夜色与海│ 静物 │ 绘画  │
z=35   └──────────────┴────────┴──────┴───────┘
       x=0    12   24.5  28.5  42.5 50.5     60
```

门厅是枢纽，有四扇门：西大门、东通主廊、北通荷兰厅、南通巴比松厅。
展厅之间**不直接连通**——动线永远是「展厅 → 走廊/门厅 → 展厅」。

---

## 五、模块结构

```
src/
├── main.js        784 行  主循环、交互编排、UI 事件、相机滑移
├── room.js       1037 行  整馆几何构建（墙/地/顶/画框/长凳/家具/雕塑）+ 按厅分组
├── textures.js    546 行  程序化纹理 + 画作双档加载（挂墙 640 / 详情 1200）
├── style.css      984 行  全部样式
├── controls.js    270 行  键鼠控制、指针锁定、拖动转视角
├── audio.js       470 行  Web Audio 合成（底噪/脚步/混响 + 生成式背景音乐）
├── plan.js        332 行  平面图编译：相邻检测、门洞、碰撞体、按厅视线剔除
├── interact.js    142 行  射线拾取（看画、坐长凳；只命中可见的厅）
├── lights.js      134 行  灯光预算与按距离剔除
├── minimap.js     120 行  左下角导览小地图（Canvas 2D）
├── flashlight.js   97 行  手电筒
├── daylight.js     89 行  光线随时间（正午 → 闭馆）
├── loader.js       57 行  数据与画作的流式加载
├── player.js       56 行  玩家状态
├── scene.js        36 行  场景/相机/渲染器单例
└── config.js       16 行  全局常量
```

**数据流**：`museum.json`（平面 + 画作清单）→ `plan.js` 编译成房间/门洞/碰撞
→ `room.js` 建几何 → `main.js` 跑主循环。

---

## 六、工具链

`tools/` 下的脚本构成一条从「拉数据」到「自检」的流水线：

| 脚本 | 作用 |
|---|---|
| `fetch-artworks.py` | 按主题从克利夫兰 API 抓画，下载并压到 1200px WebP |
| `enrich-artworks.py` | 补齐展签元数据（策展介绍、尺寸、入藏来源、可排序年代） |
| `build-galleries.py` | 把画作清单编译成 `museum.json`（平面、挂画、家具、灯光） |
| `to-webp.py` | 把画作转 WebP（可反复跑，支持传长边） |
| `make-wall-textures.py` | 从 1200px 源图生成挂墙用的 640px 缩图（源图没改就跳过） |
| `fetch-sculpture.py` / `shrink-sculpture.py` | 抓雕塑扫描件并压缩 GLB |
| `make-og-image.py` | 从实拍生成分享预览图（1200×630） |
| `check-plan.mjs` | **平面自检**：列出所有门洞，逐幅检查画有没有挂在门洞里 |
| `artworks.json` | 画作清单（唯一事实来源，改内容改这个） |

常用命令：

```bash
# 改完平面或画作后，完整重跑
python3 tools/build-galleries.py tools/artworks.json public/data/museum.json
node tools/check-plan.mjs        # 平面自检，有冲突会非零退出
python3 tools/make-wall-textures.py   # 重出挂墙 640px 缩图
node node_modules/vite/bin/vite.js build

# 只重抓某个展厅
python3 tools/fetch-artworks.py public/art tools/artworks.json dutch
python3 tools/enrich-artworks.py tools/artworks.json
```

---

## 七、性能与网络

### 画作加载

画作是最大的资源。**双档**：挂墙显示用 640px（2.2 MB，进馆即加载），
详情浮层才取 1200px（8.1 MB，点开哪幅取哪幅）——挂墙的传输量因此比原来少了七成。

加载策略：

1. **分两批**：每厅前 3 幅（主墙，共 27 幅）先来，其余随后补
2. **并发限 6**：实测一次放 72 个请求只会互相挤、集体超时
3. **失败不重试**：全部是本地相对路径，失败说明构建或文件有问题，
   单张失败只换成程序化兜底纹理、`console.warn` 一条，不拖住整面墙
4. **相关作品缩略图复用挂墙小图**：卡片只有 78×56，直接拿已在缓存里的 640px，
   不再发请求

`public/` 下的资源统一走**内容 hash**（`?v=` 是文件内容的 sha1 前 8 位），
`vite.config.js` 在构建时扫一遍 `public/` 生成对照表，`museum.json` 用的是
整个数据文件的 hash（`__BUILD_ID__`）。换图改数据后 URL 必变，浏览器不会吃旧缓存。

### 渲染

- 像素比封顶 1.5（Retina 上肉眼无差，帧率翻倍）
- 阴影贴图关闭（画面上几乎看不出来，但进厅会卡一帧）
- 灯光按距离剔除，同时点亮不超过 16 盏
- **按厅剔除**：`plan.visibleRoomsFrom()` 用二维线段求交判断哪些厅看得见，
  看不见的厅整个 group 不进渲染。12 个机位下「剔除开/关」截图逐像素比对**零差异**，
  绘制调用在厅内视角从 345 降到 165（最高降 52%），代价是每次重算 ~0.5ms，
  且只在走动 >0.15m / 转头 >1.7° / 静止 0.5s 时才重算
- 画作纹理 640px 挂墙 / 1200px 详情（详情浮层显示高度约 608px，1200 已有余量）

---

## 八、交互一览

| 操作 | 效果 |
|---|---|
| `W` `A` `S` `D` | 走动 |
| 鼠标 | 转动视角（点画面进入沉浸模式） |
| `E` | 看向画作时打开详情；走到长凳前坐下 |
| `F` | 手电筒 |
| `M` | 声音开关 |
| `B` | 背景音乐开关 |
| `H` | 操作说明面板 |
| `ESC` | 关闭浮层 / 起身 / 退出沉浸模式 |

界面上还有：右上角展厅列表（🏛）、人物显示开关（👤）、操作说明（?）；
左下角导览小地图；右下角光线时间滑块。

展厅列表和详情浮层里的「相关作品」点下去是**相机滑过去**：在房间图上 BFS 出
一条路（门厅 ↔ 主廊 ↔ 展厅），以门洞中心为途经点，0.6–1.0s 缓动滑到位，
全程不落地、不切镜头，小地图上能看见自己在走。

---

## 九、已知限制

1. **国内访问慢** —— 托管在 GitHub Pages，从国内访问只有 1–11 KB/s。
   资源已经全部本地化并分档（挂墙 640px 只有 2.2 MB，1200px 点开哪幅才取哪幅），
   但首屏仍要拉几 MB。要根治得换国内可访问的托管（对象存储 + CDN 之类）。

2. **移动端从未实测** —— 代码里有摇杆和触摸控制，但没在真机上验证过。
   用户已决定暂时不做。

3. **无障碍缺失** —— 作品信息只在 3D 场景里，屏幕阅读器读不到。

4. **部分展签数据不全** —— 少数画作缺策展介绍（barbizon 3/8、american 2/8）
   或尺寸（浮世绘版画 0/8）。这是克利夫兰馆方数据本身的缺口，
   详情浮层会跳过空字段。

5. **画作版权** —— 全部为 CC0 公版，来源与授权见 `CREDITS.md`。

6. **按厅剔除是采样判定** —— `plan.visibleRoomsFrom()` 把「这个厅有没有东西
   被看见」化简成对一组采样点（房间内部 1.6m 网格 + 门洞线每 2cm 一点 +
   场景物件坐标）做二维线段求交。随机 6 个种子 × 约 110 万次
   「看得见任一点 ⇒ 该厅必须可见」的检查全部通过，但理论上窗口比 2cm 更窄的
   擦边视线仍可能漏判成出洞。

---

## 十、部署

推送到 `main` 分支后，GitHub Actions 自动构建并发布到 GitHub Pages。

```bash
git push origin main
gh run list --limit 1     # 看构建状态
```

`vite.config.js` 里的 `base` 是 `/art-museum/`，对应仓库名。
换仓库名或换托管时，这个和 `index.html` 里的 `og:image` 绝对地址要一起改。

---

## 十一、改造历程

项目从「一个空房间」做到现在的规模，主要节点：

1. 抓 40 幅公版画作，搭起单房间场景
2. 从单房间重构成整馆平面（`plan.js` 平面编译器）
3. 连续行走、灯光剔除、标识系统、走廊展签、长凳、雕塑端景
4. 作品详情浮层 + 相关作品
5. 展厅加隔断墙（L 形动线）、墙面抹灰纹理、体积光
6. 画作转 WebP、雕塑 GLB 压缩、画作分级加载
7. 门厅补内容与灯槽
8. 修图片缓存问题（`public/` 资源加构建 ID）
9. 定位到真正瓶颈是网络，画作改走 jsDelivr
10. 从 5 个展厅扩到 9 个（按画派新增四厅）
11. 修「画挂在门洞里」（展厅之间不再开门洞）
12. 加操作说明面板、og:image、光线随时间
13. 补齐新展厅的展签元数据
14. 修 9 厅扩编后的残留：meta 描述、源码注释，导览牌标题与门牌正则改为按实际厅数计算
15. 展厅列表 / 相关作品由瞬移改成相机滑移（房间图 BFS + 门洞途经点 + 缓动）
16. 按厅视线剔除：看不见的厅整个 group 不进渲染（绘制调用最高降 52%，画面零差异）
17. 删掉画作 CDN 回退与失败重试，资源统一本地相对路径
18. 画作双档：挂墙 640px / 详情 1200px（挂墙传输量少七成）
19. `public/` 资源改成内容 hash（`?v=` = 文件 sha1 前 8 位）
